# n8n production webhook that powers the live "Gotcha" missed-call simulation.
# Copy this file to .env and set the real URL locally — .env is gitignored.
VITE_N8N_WEBHOOK_URL=https://your-instance.app.n8n.cloud/webhook/your-webhook-id
